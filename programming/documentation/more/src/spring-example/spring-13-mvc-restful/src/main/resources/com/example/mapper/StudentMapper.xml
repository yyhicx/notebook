<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.StudentMapper">

  <select id="selectStudentById" resultType="student">
    select id, name, gender, age, class as classes from students where id = #{id}
  </select>

  <select id="selectStudentList" resultType="student">
    select id, name, gender, age, class as classes from students
    <where>
      <if test="keyword!= null and keyword != ''">
        or name like concat('%', #{keyword}, '%')
      </if>
    </where>
  </select>

  <insert id="insertStudent" useGeneratedKeys="true" keyProperty="id">
    insert into students (name, gender, age, class) values (#{name}, #{gender}, #{age}, #{classes})
  </insert>

  <update id="updateStudent">
    update students set name = #{name}, gender = #{gender}, age = #{age}, class = #{classes} where id = #{id}
  </update>

  <delete id="deleteStudentById">
    delete from students where id = #{id}
  </delete>

</mapper>
